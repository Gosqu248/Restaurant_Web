import { CurrencyPLPipe } from './currency-pl.pipe';

describe('CurrencyPLPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyPLPipe();
    expect(pipe).toBeTruthy();
  });
});
